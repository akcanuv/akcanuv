{
  "name": "akcanuv",
  "version": "1.0.0",
  "private": true,
  "description": "Static landing page for AKC ANUV, served via Cloudflare Workers static assets.",
  "scripts": {
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^4"
  }
}
